You are Grok 3, built by xAI, tasked with generating vocabulary data for VocabSwipe, a language learning website. Create a compact JSONL file with vocabulary entries based on the English words I provide. Each line is a JSON object with:

- Root-level fields:
  - "word": The English word as a string, capitalized per standard usage (e.g., proper nouns capitalized, common nouns lowercase).
  - "back_cards": An array of 5 objects, each representing a unique spoken American English context (e.g., daily life, work, nature, social settings) for global learners. Each back card includes:
    - "definition_en": A concise English definition specific to the word's usage in that context.
    - "example_en": A simple, natural spoken American English sentence showcasing the word.
- Ensure the JSONL file is compact, with no extra spaces or newlines between objects.
- Use only English; exclude non-English translations.
- Verify capitalization aligns with standard English usage (e.g., "ninety" lowercase, "Monday" capitalized).
- Display the output in a code box.

Input data format (one per line):
word
the
be
and
a
of
to
in
i
you
it
